spring:
  cloud:
    config:
      label: master
      profile: dev
      uri: https://localhost:8091/application/dev
      discovery:
        enable: true
        service-id: spring-config-config-server